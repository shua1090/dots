# Future Neovim Work

Items intentionally held for a later pass:

1. GitHub PR workflow: evaluate `octo.nvim` as a closer match to VSCode's GitHub Pull Requests extension, then decide whether it should replace or complement `gh-review.nvim`.
2. Git hunk workflow: expand Gitsigns bindings for preview/reset/blame/textobject workflows after Diffview has settled in.
3. Context UI: evaluate `nvim-treesitter-context` or breadcrumb/statusline context for deep files without adding persistent visual noise.
4. Terminal workflow: keep unnamed ad hoc terminals for now; revisit terminal pickers/layouts only if popup terminals become a real bottleneck.
5. Neo-tree search/filter UX: test whether Neo-tree's built-in filtering is enough or whether a separate picker-driven file browser feels better.
6. Noice polish: decide how much command/search/message routing you actually want after living with the current command palette and notification setup.
